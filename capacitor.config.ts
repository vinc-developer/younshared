import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'You N Shared',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
